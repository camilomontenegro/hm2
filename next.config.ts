import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'kudhfkhefacuocdhumwt.supabase.co', // Your Supabase project domain
      // Add other image domains if needed
    ],
  },
};

export default nextConfig;
